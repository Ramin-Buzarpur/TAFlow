"use client";
import { useState } from "react";
export function NewMessageForm() { const [msg,setMsg]=useState(""); async function submit(fd:FormData){ setMsg("در حال ارسال..."); const res=await fetch('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({participantIds:String(fd.get('participantIds')).split(',').map(x=>x.trim()).filter(Boolean),subject:fd.get('subject'),body:fd.get('body'),type:fd.get('type')})}); const j=await res.json(); setMsg(res.ok?'پیام ساخته شد.':j.message);} return <form className="form" action={submit}><input className="input" name="participantIds" placeholder="شناسه کاربران گیرنده، با کاما جدا شود"/><input className="input" name="subject" placeholder="موضوع"/><select className="select" name="type" defaultValue="COURSE_GENERAL"><option value="COURSE_GENERAL">سوال درسی</option><option value="GRADE_APPEAL">اعتراض به نمره</option><option value="ADMIN_SUPPORT">پشتیبانی آموزش</option></select><textarea className="textarea" name="body" placeholder="متن پیام"/><button className="btn btn-primary">ارسال</button>{msg?<p className="muted">{msg}</p>:null}</form> }
