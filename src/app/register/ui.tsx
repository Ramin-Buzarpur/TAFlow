"use client";
import { useState } from "react";
export function RegisterForm(){const [msg,setMsg]=useState(''); async function submit(fd:FormData){const res=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:fd.get('name'),email:fd.get('email'),password:fd.get('password'),studentNumber:fd.get('studentNumber')})}); const j=await res.json(); setMsg(res.ok?'ثبت‌نام انجام شد.':j.message)} return <form className="form" action={submit}><input className="input" name="name" placeholder="نام"/><input className="input" name="email" type="email" placeholder="ایمیل"/><input className="input" name="studentNumber" placeholder="شماره دانشجویی"/><input className="input" name="password" type="password" placeholder="رمز عبور قوی"/><button className="btn btn-primary">ثبت‌نام</button>{msg?<p className="muted">{msg}</p>:null}</form>}
