"use client";
import { useState } from "react";
export function GradebookForms({courseOfferingId}:{courseOfferingId:string}){const [msg,setMsg]=useState(''); async function cat(fd:FormData){const res=await fetch('/api/gradebook/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({courseOfferingId,name:fd.get('name'),weight:Number(fd.get('weight')),maxScore:Number(fd.get('maxScore'))})}); const j=await res.json(); setMsg(res.ok?'دسته ساخته شد.':j.message)} return <form className="form" action={cat}><input className="input" name="name" placeholder="نام دسته مثل تمرین‌ها"/><input className="input" name="weight" placeholder="وزن"/><input className="input" name="maxScore" placeholder="حداکثر نمره"/><button className="btn btn-primary">ساخت دسته نمره</button>{msg?<p className="muted">{msg}</p>:null}</form>}
