"use client";
import { useState } from "react";
export function AnnouncementForm(){const [msg,setMsg]=useState(''); async function submit(fd:FormData){const res=await fetch('/api/announcements',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:fd.get('title'),body:fd.get('body'),priority:fd.get('priority'),courseOfferingId:fd.get('courseOfferingId')||undefined})}); const j=await res.json(); setMsg(res.ok?'اطلاعیه ثبت شد.':j.message)} return <form className="form" action={submit}><input className="input" name="courseOfferingId" placeholder="CourseOffering ID اختیاری"/><input className="input" name="title" placeholder="عنوان"/><select className="select" name="priority"><option value="normal">عادی</option><option value="important">مهم</option><option value="urgent">فوری</option></select><textarea className="textarea" name="body" placeholder="متن اطلاعیه"/><button className="btn btn-primary">انتشار</button>{msg?<p className="muted">{msg}</p>:null}</form>}
